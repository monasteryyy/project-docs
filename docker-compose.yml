services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: monastery
      POSTGRES_PASSWORD: 123456789
      POSTGRES_DB: monastery
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data

volumes:
  postgres_data: